import { Theme } from "@mui/material";

export const infoNameStyle = (theme: Theme) => {
  return { color: theme.palette.secondary.main, paddingBottom: 0, width: "fit-content", fontSize: 15, fontFamily: "system-ui" };
};

export const infoValueStyle = (theme: Theme) => {
  return { color: theme.palette.secondary.main, paddingBottom: 0, width: "fit-content", fontSize: 15, fontFamily: "system-ui" };
};
